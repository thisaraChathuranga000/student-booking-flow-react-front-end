services:
  - type: web
    name: student-booking-flow
    runtime: static
    buildCommand: npm run build
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html